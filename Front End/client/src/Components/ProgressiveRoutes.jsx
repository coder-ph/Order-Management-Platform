// import { useSelector } from "react-redux";
// import { Navigate, Outlet } from "react-router-dom";

// const ProgressiveRoute = ({ requiredStep, nextStep }) => {
//   const currentStep = useSelector((state) => state.user.currentStep); // Assuming you track user progress in Redux

//   if (currentStep < requiredStep) {
//     return <Navigate to={nextStep} replace />; 
//   }

//   return <Outlet />; 
// };

// export default ProgressiveRoute;
